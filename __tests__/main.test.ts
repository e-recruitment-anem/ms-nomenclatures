describe("auth", () => {
  describe("get auth route", () => {
    describe("given the first", () => {
      it("should return a successful test", async () => {
        const testCase = true;
        expect(testCase).toBe(true);
      });
    });
  });
});
